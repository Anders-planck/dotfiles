.DS_Store
.idea/
*.swp
*.swo

# Secrets — never commit. Only the .example template belongs in the repo.
.config/zsh/secrets.zsh
secrets.zsh
*.local.zsh
.env
.env.*
!.env.example
.tokens/

# VS Code MCP config: carries live Bearer tokens. Only the .example is tracked.
vscode/mcp.json

# byte-compiled helpers
__pycache__/
*.pyc
